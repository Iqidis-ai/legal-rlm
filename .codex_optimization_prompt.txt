You are a performance + cost optimization reviewer for Irys RLM. The team is about to ship LLM-cost visibility; in parallel I want your thoughts on OTHER optimization opportunities they should chase — not just inference costs. Think broadly.

The system is a legal intelligence platform:
- Per-matter SQLite DB (WAL mode, STRICT tables)
- Gemini API (NANO/LITE/FLASH/PRO tiers) with context caching
- Gradio UI with an InProcessBackend
- FastAPI service with 67 endpoints
- ~6900-line engine.py, 7000-line matter.py, 5500-line graph.py
- Recent work: P0.1 provenance, P0.2 trust-aware reads, P0.3 review queue, P0.4 trust invalidation, P0.5 content policy

READ NARROWLY to orient (do not load full files):
- `src/irys/rlm/engine.py` grep for the main loop shape and the hot-path methods: `investigate`, `_orient`, `_analyze_search_results`, `_deep_read_document`, `_synthesize`, `_assemble_context_packet`, `_hydrate_from_matter_model`, `_search_cached_assertions`.
- `src/irys/matter/graph.py` class names only.
- `src/irys/core/search.py` grep for function names.
- Look at reasoning_cache behavior + trust_revision keying.
- Look at how the UI refreshes panels (panel handler wiring in `src/irys/ui/app.py`).

Produce a ranked list of optimization opportunities BEYOND raw LLM token cost. Examples of categories to consider:

1. Hot-path DB queries. Any N+1 patterns in list_assertions, review_queue, coverage_report? Any missing indexes? Any joins that could be CTEs? Any subqueries computed per-row?

2. Cache hit rates beyond Gemini. Reasoning cache is trust-revision-keyed now. Is the cache actually hitting during repeated queries? If the key is too specific, the SO-1 reuse rate drops. If too loose, stale state leaks. Probe.

3. UI refresh storms. Right after a verify/reject, the handler refreshes 5+ panels. Are those panels making redundant DB reads? Could they share a single overview fetch?

4. Engine orientation cold-start. Orient builds a file listing + semantic gate. Does it re-scan everything every run? Is any of this reuseable from prior runs of the same matter?

5. Ingest path efficiency. Document read -> profile -> deep-read -> extract. Are any stages doing redundant I/O? Is `inventory.upsert` being called where update_hash alone would suffice?

6. Trust revision bumps. P0.4 bumps trust_revision on every rejection. A reviewer going through 50 candidates bumps 50 times. Does that invalidate 50 caches unnecessarily? Should bumps be coalesced?

7. Memory and CPU on the UI path. Gradio refreshes the whole panel on each action. For a 200-row review queue, is there unnecessary HTML regeneration?

8. Background maintenance hooks. Once Phase 0 closes, the product wants background loops doing maintenance. What's the cheapest shape — cron, queue, on-idle?

9. Anything else you spot that the team hasn't considered. Cold-path ingest, parser overhead (PyMuPDF), test harness, import times, schema migration runtime.

Give a single page. Ranked list with estimated impact (high/medium/low) and rough effort (S/M/L). Concrete file:line pointers preferred. This is a brainstorm pass — not a review — so be expansive about what TO look at, not just what's broken.

Keep under 1500 words.
