**Must Fix**
- `load_post_review_snapshot()` launders per-read failures into false UI states. If `count_review_queue()` fails, `_safe()` substitutes `{"total": 0}` and `_fmt_review_count_badge()` renders “✓ All findings reviewed”; if `get_review_queue()` fails, `_safe()` substitutes `[]` and `_fmt_review_queue()` says “No findings need review.” The same pattern turns `issues` / `assertions` / `overview` failures into empty panels, whereas the old per-panel loaders surfaced explicit errors. `src/irys/ui/app.py:1951-1962`, `src/irys/ui/app.py:2009-2015`, `src/irys/ui/app.py:3051-3092`, compare `src/irys/ui/app.py:2851-2902`

**Should Fix**
- Add a parity test for the new count path. The SQL looks equivalent now, but `count_review_queue()` duplicates the bucket CASE separately and currently has no direct regression test; the existing suite only exercises `get_review_queue()` ordering/buckets. `src/irys/matter/graph.py:5791-5869`, `tests/matter/test_review_queue.py:64-105`, `tests/matter/test_review_queue.py:233-259`

**Nice**
- `asyncio.gather()` is not true in-process read concurrency today: the gathered `InProcessBackend` coroutines have no internal `await`, so they execute synchronously on one thread. That means I do not see a race here, and the service-side `_get_matter_model_or_404()` rehydrate path is lock-protected anyway; the real win is collapsing seven `_run_async` spawns to one, not parallel SQLite reads. `src/irys/ui/app.py:3034-3048`, `src/irys/ui/backends/in_process.py:565-576`, `src/irys/ui/backends/in_process.py:622-641`, `src/irys/service/api.py:345-377`
- No obvious missing-index blocker for a 20k-candidate count query: the hot predicates already have supporting indexes on `verification_state`, `evidence_edge`, `gap_link`, `issue_predicate`, and contradicted-link lookup. `src/irys/matter/schema.py:2452-2457`, `src/irys/matter/schema.py:724-728`, `src/irys/matter/schema.py:634-637`, `src/irys/matter/schema.py:587-588`, `src/irys/matter/schema.py:1581-1583`

I did not find bucket drift: bucket 3 depends only on `vs.target_kind='issue_predicate'`, so dropping `predicate_issue` from the count query does not change assignments, and a direct in-memory parity check matched `review_queue()` exactly. Dropdown reset still matches the old `load_review_queue()` behavior, and the drawer still reads the prior target’s provenance/events after the write because those queries are keyed by target kind/id, not candidate status. `src/irys/matter/graph.py:5747-5758`, `src/irys/matter/graph.py:5839-5850`, `src/irys/ui/app.py:2900-2902`, `src/irys/matter/graph.py:5565-5574`, `src/irys/matter/graph.py:5940-5953`, `src/irys/matter/graph.py:6266-6339`