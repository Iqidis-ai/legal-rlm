You are a senior architect. Read .claude/CLAUDE.md, src/irys/matter/graph.py, src/irys/matter/schema.py, src/irys/matter/matter.py, src/irys/rlm/engine.py (focus on _orient and _deep_read_document methods).

Design a Document Intelligence Layer for this legal analysis system. We need:
1. Per-document intelligence cards (what IS each doc, key clauses, parties, role in matter)
2. Document relationship map (references, supersedes, modifies, contradicts)
3. Cross-document contradiction detection
4. File change detection and re-indexing
5. User annotation of document-level intelligence
6. Integration with existing stores (DocumentInventoryStore, assertions, trust_overrides)

CRITICAL CONSTRAINT: The investigation pipeline must be INVERTED from current design. Currently it searches first then reads. It must READ documents first (guided by metadata/document cards), THEN search for targeted follow-ups. No small-repo vs big-repo branching.

Give exact schema DDL, method signatures with file paths, and implementation order. Reuse existing infrastructure aggressively.
