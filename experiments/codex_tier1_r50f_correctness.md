CLEAN.

1. Rehydration race: the comment at [api.py:293](C:/Users/devan/OneDrive/Desktop/Projects/legal-rlm/src/irys/service/api.py:293) is correct in this codebase. After `await asyncio.to_thread(...)` resumes, there is no further `await`, and `_active_matter_models` is only mutated from async paths on the event-loop thread, so another asyncio task cannot slip in between `get()` and assignment. That closes the in-process race where two coroutines could both publish and return different live models. It does not stop redundant concurrent rehydrate work in worker threads; it stops duplicate active-model registration.

2. `db.close()` revert: the reasoning is also correct. In file-backed mode, `SQLiteMatterDB.close()` only closes the caller thread’s `self._local.conn` [db.py:15](C:/Users/devan/OneDrive/Desktop/Projects/legal-rlm/src/irys/matter/db.py:15), so calling it from the cleanup coroutine in [api.py:116](C:/Users/devan/OneDrive/Desktop/Projects/legal-rlm/src/irys/service/api.py:116) could never close background-flush thread handles, and those flush threads are explicitly separate in [api.py:2071](C:/Users/devan/OneDrive/Desktop/Projects/legal-rlm/src/irys/service/api.py:2071). For this design, GC plus thread exit is sufficient: eviction only drops the registry reference, so in-flight users keep the model alive until done, and background flush threads release their thread-local connections when they exit. The only caveat is that the `to_thread` rehydration worker’s connection also relies on object reclamation rather than thread exit, so cleanup is not perfectly deterministic, but that is not a correctness blocker here.

Static review only; no tests run.