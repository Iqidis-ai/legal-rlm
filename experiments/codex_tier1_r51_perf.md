1. MEDIUM: The cleanup-loop guard added for shared `matter_id`s makes each sweep potentially quadratic in retained jobs. `_cleanup_loop()` builds `expired`, then for every expired job does a fresh `any(... for jid, j in _jobs.items())` scan to see whether another non-expired job still points at the same matter. With a busy service retaining many completed jobs for `cleanup_after_seconds`, that turns the once-per-minute sweep into `O(expired * jobs)` work and can create avoidable CPU spikes. Precomputing a per-`matter_id` live-job refcount once per pass would remove the repeated rescans. [src/irys/service/api.py:91](C:/Users/devan/OneDrive/Desktop/Projects/legal-rlm/src/irys/service/api.py:91) [src/irys/service/api.py:113](C:/Users/devan/OneDrive/Desktop/Projects/legal-rlm/src/irys/service/api.py:113)

2. MEDIUM: Cold rehydration is no longer event-loop blocking, but it is still an uncached full scan, and concurrent misses duplicate all of that work. On a miss, `_get_matter_model_or_404()` sends every request into `_try_rehydrate_matter_model()` via `asyncio.to_thread()`. The recent race fix only deduplicates after the thread returns, so simultaneous requests for the same inactive or nonexistent matter still each walk every corpus directory and probe SQLite until match/exhaustion. That scales with corpus count, consumes threadpool slots, and adds repeated disk I/O under fan-in. A persisted `matter_id -> db_path` index, negative cache, or in-flight rehydration dedupe would remove the `O(corpus_dirs)` miss path. [src/irys/service/api.py:255](C:/Users/devan/OneDrive/Desktop/Projects/legal-rlm/src/irys/service/api.py:255) [src/irys/service/api.py:289](C:/Users/devan/OneDrive/Desktop/Projects/legal-rlm/src/irys/service/api.py:289) [src/irys/service/api.py:298](C:/Users/devan/OneDrive/Desktop/Projects/legal-rlm/src/irys/service/api.py:298) [src/irys/matter/db.py:24](C:/Users/devan/OneDrive/Desktop/Projects/legal-rlm/src/irys/matter/db.py:24)

CLEAN for the other requested focus areas. I did not find a fresh Tier 1 performance issue in `_unique_display_name` / `_format_search_results` or in `set_trust_override` / `delete_trust_override`; the recent dedupe, targeted recompute, and override preloading changes removed the earlier wasted work there.

Reviewed commits: `116a5ba`, `2369d99`, `2cf6144`, `3f5050b`, `766b98c`, `d09414b`, `3937782`. Accepted risks were excluded as requested.