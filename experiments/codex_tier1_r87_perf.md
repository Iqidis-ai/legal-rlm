CLEAN

No Tier 1 performance findings in [_resolve_resumed_run_id()` in `api.py`](/C:/Users/devan/OneDrive/Desktop/Projects/legal-rlm/src/irys/service/api.py#L1507). The path is a PK lookup through [`ledger.get_run()`](/C:/Users/devan/OneDrive/Desktop/Projects/legal-rlm/src/irys/matter/reasoning.py#L305) plus a scoped `SELECT id ... fetchone()` on `run_session`; with [`ix_run_matter(matter_id, status, started_at)`](/C:/Users/devan/OneDrive/Desktop/Projects/legal-rlm/src/irys/matter/schema.py#L165) and the existing guard against another active steerable run in the matter ([`api.py`](/C:/Users/devan/OneDrive/Desktop/Projects/legal-rlm/src/irys/service/api.py#L2853)), that lookup is operating over a tiny active subset, so it is efficient enough. Using `fetchone()` here is fine; I would not flag the lack of `LIMIT 1` as a performance issue on this control path.

The broad `try/except` in [`api.py`](/C:/Users/devan/OneDrive/Desktop/Projects/legal-rlm/src/irys/service/api.py#L1516) is not a performance concern unless it starts throwing frequently, and if that happens the real issue is DB/schema health or observability, not the `try` itself. Residual risk only: there is no `resumed_from`-keyed index, so if the system later allows many simultaneous `running` sessions per matter, this would degrade to a linear scan over that active subset. Static review only; no runtime profiling was run.