**Findings**
- HIGH — The new guard still cannot prove that the selected running row is the continuation of the interrupted run being steered. `_resolve_active_run_id()` now rewrites any interrupted `run_id` to “the only running non-utility run in this matter” when `len(active_rows) == 1` [in_process.py](C:/Users/devan/OneDrive/Desktop/Projects/legal-rlm/src/irys/ui/backends/in_process.py#L140), but `run_session` has no parent/resume-lineage field to justify that assumption [schema.py](C:/Users/devan/OneDrive/Desktop/Projects/legal-rlm/src/irys/matter/schema.py#L150). Since redirects are explicitly allowed on interrupted runs before resume [reasoning.py](C:/Users/devan/OneDrive/Desktop/Projects/legal-rlm/src/irys/matter/reasoning.py#L254), an interrupted run `A` plus one unrelated running run `B` in the same matter can still make stop/redirect hit `B`. So this narrows the r83 bug, but does not fully close the wrong-targeting class.
- MEDIUM — The known r83 architectural gap is still open. `do_resume()` leaves `current_run_id` stale until the background resume finishes [app.py](C:/Users/devan/OneDrive/Desktop/Projects/legal-rlm/src/irys/ui/app.py#L635), while the UI continues to populate steering controls from that field [app.py](C:/Users/devan/OneDrive/Desktop/Projects/legal-rlm/src/irys/ui/app.py#L888). `InProcessBackend` now compensates best-effort, but the HTTP backend still forwards the supplied `run_id` verbatim [http.py](C:/Users/devan/OneDrive/Desktop/Projects/legal-rlm/src/irys/ui/backends/http.py#L61), and the service run-scoped stop/redirect endpoints still trust it verbatim [service/api.py](C:/Users/devan/OneDrive/Desktop/Projects/legal-rlm/src/irys/service/api.py#L1550) [service/api.py](C:/Users/devan/OneDrive/Desktop/Projects/legal-rlm/src/irys/service/api.py#L2722). That MEDIUM remains unchanged.
- LOW — No new files were added, but the touched backend still contains dead state: `_event_queues` and `_active_runs` are declared and never referenced elsewhere [in_process.py](C:/Users/devan/OneDrive/Desktop/Projects/legal-rlm/src/irys/ui/backends/in_process.py#L79). Pre-existing entropy, not introduced by this diff.

`fetchall()` + the exactly-one guard does fix the specific r83 `fetchone()` issue for the `>1 running rows` case, and zero-row / select→update race cases fail closed because `request_stop()` / `request_redirect()` are status-guarded. `matter_id` scoping is correct. But the answer to “fully closed?” is still no, because “exactly one running row in the matter” is not the same as “the resumed child of this interrupted run.”

Static review only; I did not run tests.