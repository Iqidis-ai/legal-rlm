PASS

LOW:
1. The new recovery query in [_revise_one()](/C:/Users/devan/OneDrive/Desktop/Projects/legal-rlm/src/irys/matter/belief_revision.py#L504) should not do a full `assertion_occurrence` table scan, because `assertion_id=?` is indexable. But the `ORDER BY CASE speech_act ...` is not aligned with either existing index, so SQLite cannot use an index to satisfy the ordering. [schema.py](/C:/Users/devan/OneDrive/Desktop/Projects/legal-rlm/src/irys/matter/schema.py#L64) [schema.py](/C:/Users/devan/OneDrive/Desktop/Projects/legal-rlm/src/irys/matter/schema.py#L67) `ix_occurrence_assertion_created(assertion_id, created_at)` only helps on the leading `assertion_id` filter here; it does not cover the sort, and it does not cover `speech_act`. The better candidate is actually `ix_occurrence_assertion(assertion_id, speech_act)`, which is covering for the read, but it still cannot satisfy the CASE ordering. Net: per-assertion range scan plus sort/rank of matching rows, not whole-table scan.

2. The r15 `elif cause == USER_CORRECTION` path adds one extra append-only row to `assertion_revision` for same-state user corrections. [_revise_one()/force_state()](/C:/Users/devan/OneDrive/Desktop/Projects/legal-rlm/src/irys/matter/belief_revision.py#L714) [write_revision_rows()](/C:/Users/devan/OneDrive/Desktop/Projects/legal-rlm/src/irys/matter/graph.py#L279) [schema.py](/C:/Users/devan/OneDrive/Desktop/Projects/legal-rlm/src/irys/matter/schema.py#L143) That means one extra PK insert and one extra secondary-index entry. I do not see this as meaningful churn at Tier 1 scale because it is only on manual same-state corrections, not BFS hot-path traffic. SQLite maintains the table/index B-trees automatically on insert, but this repo does not configure `auto_vacuum`, manual `VACUUM`, `ANALYZE`, or `PRAGMA optimize`. [db.py](/C:/Users/devan/OneDrive/Desktop/Projects/legal-rlm/src/irys/matter/db.py#L58) Since `assertion_revision` is append-only here, lack of VACUUM is not a current performance concern.

3. No new N+1 pattern was introduced in r15 relative to r14. The narrow recovery-path per-node queries were already present in r14; r15 only changed the occurrence lookup from `ORDER BY created_at` to `ORDER BY CASE speech_act ...` and made the user-lock audit row always get written. [experiments/codex_tier1_r14_perf.md](/C:/Users/devan/OneDrive/Desktop/Projects/legal-rlm/experiments/codex_tier1_r14_perf.md) The main BFS neighbor fetch is still batched through [get_neighbor_belief_states()](/C:/Users/devan/OneDrive/Desktop/Projects/legal-rlm/src/irys/matter/graph.py#L436), so there is no new broad hot-path N+1.

4. There is a real but LOW risk from the CASE expression: it prevents SQLite from using index order for `ORDER BY`, so `LIMIT 1` does not become an early index stop. It does not prevent index usage entirely, because the `WHERE assertion_id=?` predicate is still sargable. The practical consequence is “scan all occurrences for this assertion and rank them,” not “scan the whole table.” Given this only runs in the supersession-recovery branch, not the normal BFS path, I would keep this as LOW.

I could not run `EXPLAIN QUERY PLAN` in this sandbox because executable invocation is blocked, so the index/planner conclusions above are based on SQLite index rules and the checked-in schema.