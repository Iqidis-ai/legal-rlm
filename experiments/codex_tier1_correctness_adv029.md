Issues found in `185d882`:

- HIGH: the new durable queue is deleted before the deferred work is actually replayed, so it is not crash-safe. Both [`drain_correction_pending()`](/C:/Users/devan/OneDrive/Desktop/Projects/legal-rlm/src/irys/matter/matter.py#L308) and [`drain_evidence_pending()`](/C:/Users/devan/OneDrive/Desktop/Projects/legal-rlm/src/irys/matter/matter.py#L361) clear memory and `DELETE` the persisted rows immediately, then [`flush_revisions()`](/C:/Users/devan/OneDrive/Desktop/Projects/legal-rlm/src/irys/matter/runtime.py#L440) processes the returned seeds afterward. Any crash or exception between the drain and the end of replay loses the queue contents permanently. The same queue methods also swallow DB exceptions, so durability can silently degrade to in-memory only.

- HIGH: `/matter/{matter_id}/flush-pending` is broken on the nontrivial path because it builds `MatterRuntimeAdapter(..., run_id=None)` in [`api.py`](/C:/Users/devan/OneDrive/Desktop/Projects/legal-rlm/src/irys/service/api.py#L1526), but [`flush_revisions()`](/C:/Users/devan/OneDrive/Desktop/Projects/legal-rlm/src/irys/matter/runtime.py#L470) still unconditionally calls `ledger.append_event(run_id=self.run_id, ...)` for deferred-correction attribution and revised assertions. [`ReasoningLedgerStore.append_event()`](/C:/Users/devan/OneDrive/Desktop/Projects/legal-rlm/src/irys/matter/reasoning.py#L75) inserts into `ledger_event.run_id`, and the schema requires that column to be `NOT NULL REFERENCES run_session(id)` in [`schema.py`](/C:/Users/devan/OneDrive/Desktop/Projects/legal-rlm/src/irys/matter/schema.py#L167). So the standalone flush will 500 as soon as it has anything to log. Because of the eager-delete bug above, that failure also discards the drained pending rows.

- MEDIUM: a standalone flush that truncates again destroys the original provenance on requeue. In [`flush_revisions()`](/C:/Users/devan/OneDrive/Desktop/Projects/legal-rlm/src/irys/matter/runtime.py#L489), second-level correction truncation is re-enqueued with `run_id=self.run_id`, and evidence truncation does the same at [`runtime.py`](/C:/Users/devan/OneDrive/Desktop/Projects/legal-rlm/src/irys/matter/runtime.py#L546). On the new endpoint `self.run_id` is `None`, so the previously persisted `orig_run_id` from `_correction_map` / `_evidence_carry` is dropped after one truncated standalone replay.

- LOW: `_load_pending_propagation()` silently coerces any unrecognized evidence `cause` to `RevisionCause.NEW_EVIDENCE` in [`matter.py`](/C:/Users/devan/OneDrive/Desktop/Projects/legal-rlm/src/irys/matter/matter.py#L119). That keeps replay moving, but it also rewrites semantics and audit provenance for malformed or stale rows instead of surfacing them.

I did not find a separate correctness bug in the new proof-state recompute block itself: empty `_revised_ids` short-circuits correctly, the exception path is contained, and the batched proof-state writes are transaction-scoped. The lock+SQLite-WAL pattern is also reasonable for one shared `MatterModel` instance; the real problems are the eager-delete queue lifecycle and the null-`run_id` standalone flush path.