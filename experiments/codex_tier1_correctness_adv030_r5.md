NOT CLEAN

1. High: run-scoped stop/redirect are still cross-matter and still accept utility runs by exact `run_id`. [api.py](/C:/Users/devan/OneDrive/Desktop/Projects/legal-rlm/src/irys/service/api.py#L1390) and [api.py](/C:/Users/devan/OneDrive/Desktop/Projects/legal-rlm/src/irys/service/api.py#L2432) call `model.ledger.get_run(run_id)` and then `request_redirect` / `request_stop` without checking `run.matter_id == matter_id` or rejecting `manual_flush` / `background_flush`. The ledger store itself is keyed only by `id` in [reasoning.py](/C:/Users/devan/OneDrive/Desktop/Projects/legal-rlm/src/irys/matter/reasoning.py#L201) and [reasoning.py](/C:/Users/devan/OneDrive/Desktop/Projects/legal-rlm/src/irys/matter/reasoning.py#L246). A caller can therefore stop or redirect a run from another matter, including a utility flush run, via the wrong matter path.

2. High: `set_trust_override()` is still not hardened at the model layer, so the API fix is bypassable by race/direct call. The endpoint now selects a filtered `run_id` in [api.py](/C:/Users/devan/OneDrive/Desktop/Projects/legal-rlm/src/irys/service/api.py#L1457), but `MatterModel.set_trust_override()` blindly reuses whatever `run_id` it receives in [matter.py](/C:/Users/devan/OneDrive/Desktop/Projects/legal-rlm/src/irys/matter/matter.py#L619) and forwards it into revision/enqueue in [matter.py](/C:/Users/devan/OneDrive/Desktop/Projects/legal-rlm/src/irys/matter/matter.py#L665). If the investigation finishes after selection but before the model call, or any non-REST caller passes a utility/stale run id, provenance is still attached to the wrong run.

3. Medium: `correct_assertion()` is only fully fixed on the REST/UI path, not in the underlying model path. `MatterModel.correct_assertion()` still validates only `id + matter_id + status='running'` in [matter.py](/C:/Users/devan/OneDrive/Desktop/Projects/legal-rlm/src/irys/matter/matter.py#L499), so `manual_flush` / `background_flush` run ids are still accepted there. `InProcessBackend.correct_assertion()` passes any supplied `run_id` straight through in [in_process.py](/C:/Users/devan/OneDrive/Desktop/Projects/legal-rlm/src/irys/ui/backends/in_process.py#L218). That leaves utility-run filtering incomplete outside the REST endpoint.

The specific active-run selectors changed in `e3f442c` and `dead90c` do look consistent: matter-level stop, trust-override fallback, correct-assertion fallback, and the Gradio capture/stop fallback now exclude utility runs. But the full `adv#030` chain is still not clean because filtering/validation is still bypassable in the model and run-scoped control paths.