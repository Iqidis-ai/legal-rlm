**Findings**
1. Medium: a node that exhausts OCC retries and then later succeeds in the same BFS pass still leaves the pass marked truncated. [`occ_exhausted_count` increments on abandon](C:/Users/devan/OneDrive/Desktop/Projects/legal-rlm/src/irys/matter/belief_revision.py#L330), but a later success only [`discard()`s the node from `_occ_abandoned`](C:/Users/devan/OneDrive/Desktop/Projects/legal-rlm/src/irys/matter/belief_revision.py#L336); the final truncation/warning path still keys off the stale counter at [`truncated = bool(pending) or occ_exhausted_count > 0`](C:/Users/devan/OneDrive/Desktop/Projects/legal-rlm/src/irys/matter/belief_revision.py#L397). That can produce `propagation_truncated=True` and “abandoned after OCC retry cap” warnings even when `unvisited` is empty and the node already recovered.

2. Medium: truncated/OCC-abandoned work is still only drained on the `correct_assertion()` path. `BeliefRevisionEngine.apply()` can surface `unvisited`, but [`MatterModel.apply_revision()`](C:/Users/devan/OneDrive/Desktop/Projects/legal-rlm/src/irys/matter/matter.py#L221), [`Runtime.flush_revisions()`](C:/Users/devan/OneDrive/Desktop/Projects/legal-rlm/src/irys/matter/runtime.py#L460), the [trust-override path](C:/Users/devan/OneDrive/Desktop/Projects/legal-rlm/src/irys/matter/matter.py#L394), and the [quant-conflict path](C:/Users/devan/OneDrive/Desktop/Projects/legal-rlm/src/irys/matter/matter.py#L933) all call `apply()` without collecting/requeueing those nodes. So MAX_WORK/OCC-cap exhaustion still leaves stale downstream state outside user-correction flows.

**Checks**
- `_occ_retry_nodes`: the set is populated and cleared correctly. It is only added on actual re-enqueue, and cleared on the first non-aborted pass. One nuance: a node can remain in `_occ_retry_nodes` after later retry-cap exhaustion, but that is useful, not harmful, because if some parent re-enqueues it later in the same pass the eventual success still forces fan-out.
- `_occ_abandoned`: no true race inside one BFS pass; this is per-call local state in a single-threaded loop. The bug is stale accounting, not concurrent mutation.
- `dict.fromkeys`: content dedup is correct for `pending + _occ_abandoned`. Only caveat: abandoned-only order is no longer deterministic because `_occ_abandoned` is now a set.
- v39 `SAVEPOINT/RELEASE`: `_migration_v39()` itself is correct in both autocommit and nested-transaction execution. If the claim is about `apply_schema()` as a whole being safe inside an outer transaction, that is still false because it unconditionally commits at [`conn.commit()` before migration dispatch](C:/Users/devan/OneDrive/Desktop/Projects/legal-rlm/src/irys/matter/schema.py#L1580) and again after each migration at [line 1592](C:/Users/devan/OneDrive/Desktop/Projects/legal-rlm/src/irys/matter/schema.py#L1592).

No other direct correctness bug in the r20 `_occ_retry_nodes` fan-out change stood out beyond the stale truncation accounting above.