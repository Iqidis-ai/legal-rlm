CLEAN.

No HIGHs, MEDIUMs, or LOWs on this change.

Under the repo’s current SQLite usage, the read-after-fence pattern is sufficient; a `write_transaction()` wrapper is not needed for correctness here. `resume_investigation()` now fences with `clear_next_action()` before the reread on the old run ([engine.py:4814]( /C:/Users/devan/OneDrive/Desktop/Projects/legal-rlm/src/irys/rlm/engine.py#L4814 ), [engine.py:4819]( /C:/Users/devan/OneDrive/Desktop/Projects/legal-rlm/src/irys/rlm/engine.py#L4819 ), [engine.py:4820]( /C:/Users/devan/OneDrive/Desktop/Projects/legal-rlm/src/irys/rlm/engine.py#L4820 )). The relevant DB calls are plain autocommit statements (`isolation_level=None`) on WAL connections ([db.py:54]( /C:/Users/devan/OneDrive/Desktop/Projects/legal-rlm/src/irys/matter/db.py#L54 ), [db.py:58]( /C:/Users/devan/OneDrive/Desktop/Projects/legal-rlm/src/irys/matter/db.py#L58 )), and `request_redirect()` can only update an interrupted run while `next_action IS NOT NULL` ([reasoning.py:243]( /C:/Users/devan/OneDrive/Desktop/Projects/legal-rlm/src/irys/matter/reasoning.py#L243 ), [reasoning.py:258]( /C:/Users/devan/OneDrive/Desktop/Projects/legal-rlm/src/irys/matter/reasoning.py#L258 )). In SQLite WAL, separate connections only see committed transactions, and writes are serialized, so any redirect that commits before the fence is visible to the post-fence `get_run()`, and any redirect after the fence fails the `next_action IS NOT NULL` predicate. That matches the SQLite isolation docs: https://www.sqlite.org/isolation.html

The added `get_run()` is one extra single-row `SELECT` on the old run ([reasoning.py:290]( /C:/Users/devan/OneDrive/Desktop/Projects/legal-rlm/src/irys/matter/reasoning.py#L290 )). I do not see a meaningful performance concern from that. If anything, wrapping this in `write_transaction()` / `BEGIN IMMEDIATE` ([db.py:97]( /C:/Users/devan/OneDrive/Desktop/Projects/legal-rlm/src/irys/matter/db.py#L97 )) would be stronger than needed and would hold the DB-wide writer lock a bit longer.

Residual gap: I did not find a targeted regression test for the exact stop -> late redirect -> resume interleaving, so the code looks correct but that specific race is not obviously pinned down by tests.