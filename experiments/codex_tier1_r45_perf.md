1. Low: `assertion_revision.id` is independently indexed only as a separate `TEXT PRIMARY KEY` ([schema.py#L129](/C:/Users/devan/OneDrive/Desktop/Projects/legal-rlm/src/irys/matter/schema.py#L129)), but that does not make `ORDER BY ... id DESC` efficient inside `WHERE assertion_id=?`. The history query orders by `(created_at DESC, batch_id DESC, id DESC)` ([api.py#L1701](/C:/Users/devan/OneDrive/Desktop/Projects/legal-rlm/src/irys/service/api.py#L1701)), while the only matching history index is `(assertion_id, created_at DESC)` ([schema.py#L143](/C:/Users/devan/OneDrive/Desktop/Projects/legal-rlm/src/irys/matter/schema.py#L143)). SQLite will use that index for the slice/start order, but `batch_id` and `id` are still post-index tie-breakers; it will not fuse the separate PK index on `id` into this single-table ordered scan.

2. No new material perf concern from `9de6c59`. The endpoint already had the same non-covered tie-break problem because `batch_id DESC` was already in the `ORDER BY`; adding `id DESC` just makes same-`created_at`/same-`batch_id` groups deterministic. Those ties are real because revision rows in one batch share one timestamp ([graph.py#L300](/C:/Users/devan/OneDrive/Desktop/Projects/legal-rlm/src/irys/matter/graph.py#L300), [graph.py#L303](/C:/Users/devan/OneDrive/Desktop/Projects/legal-rlm/src/irys/matter/graph.py#L303)), but the incremental cost should be negligible relative to the pre-existing sort work.